version: "3.8"
services:
  recognizer:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./static:/app/static
      - ./data:/app/data
    environment:
      - EPOCHS=3
      - LEARNING_RATE=0.1
      - HIDDEN_NODES=200
      - MODEL_PATH=/app/data/model.weights
